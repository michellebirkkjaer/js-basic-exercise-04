"use strict";

const firstMessage = `I'm learning to code with JavaScript`;

console.log(`${firstMessage}`);

const firstMessage = `I want to become a good JavaScript programmer`;

console.log(`${firstMessage}`);


//man kan ikke lave to const

// let kan man senere ændre - så skal man bare ikke skrive let foran feks "firstMessage". Man skriver bare firstMessage = den nye formulering.